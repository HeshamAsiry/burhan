# Burhan Test Generator v1

Combines independently generated `mutashabihat` and `recite_range` questions into one test. v1 accepts an explicit question blueprint so generation is deterministic. Persistence creates one `tests` row and one `test_questions` row per question. Automatic selection by level, coverage, prior exposure, and target distribution is the next layer.